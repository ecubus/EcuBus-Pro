name: TypeScript Type Check

on:
  pull_request:
    branches: [master]

jobs:
  typecheck:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Build worker script API bundle
        run: npm run worker:js

      - name: Run full TypeScript type check
        run: npm run typecheck

      - name: Verify worker script API exports
        run: npm run test -- --run test/worker/workerExports.spec.ts

      - name: Verify type check results
        run: |
          echo "✅ All TypeScript type checks passed successfully"